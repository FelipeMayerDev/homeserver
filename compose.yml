services:
  discord-bot:
    build: 
      context: ./discord-bot
      dockerfile: Dockerfile
    container_name: discord-bot
    env_file:
      - ./discord-bot/.env
    volumes:
      - ./discord-bot:/app
    restart: unless-stopped
    network_mode: host

  webhook:
    build:
      context: ./webhook
      dockerfile: Dockerfile
    container_name: webhook-service
    env_file:
      - ./webhook/.env
    ports:
      - "8000:8000"
    volumes:
      - ./webhook:/app
    restart: unless-stopped